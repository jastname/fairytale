
20260408 이성민 추가
CREATE TABLE tb_nlcf_fairytale (
    fairytale_id          VARCHAR(50)   PRIMARY KEY,
    collect_id            VARCHAR(50)   NOT NULL,
    title                 VARCHAR(500)  NOT NULL,
    alternative_title     VARCHAR(500),
    creator               VARCHAR(300),
    reg_date_raw          VARCHAR(50),
    collection_db         VARCHAR(300),
    subject_category      VARCHAR(500),
    subject_keyword       VARCHAR(1000),
    extent                VARCHAR(200),
    description           TEXT          NOT NULL,
    spatial_coverage      VARCHAR(1000),
    temporal_info         VARCHAR(500),
    person_info           VARCHAR(500),
    "language"            VARCHAR(100),
    source_title          VARCHAR(500),
    reference_identifier  VARCHAR(1000),
    rights_info           VARCHAR(500),
    copyright_others      VARCHAR(1000),
    content_url           VARCHAR(1000),
    contributor           VARCHAR(500),
    use_yn                BPCHAR(1),
    reg_dt                TIMESTAMP,
    upd_dt                TIMESTAMP
);

CREATE TABLE tb_nlcf_collect (
    collect_id       VARCHAR(50)   PRIMARY KEY,
    request_url      VARCHAR(1000),
    page_no          INTEGER,
    num_of_rows      INTEGER,
    result_code      VARCHAR(20),
    result_msg       VARCHAR(200),
    success_yn       CHAR(1),
    error_message    TEXT,
    collected_count  INTEGER       DEFAULT 0,
    raw_response     TEXT,
    reg_dt           TIMESTAMP     DEFAULT CURRENT_TIMESTAMP
);

==========================================================================

20260409 이성민 추가

CREATE TABLE tb_nlcf_fairytale_embedding (
    embedding_id        VARCHAR(50) PRIMARY KEY,
    chunk_id            VARCHAR(50) NOT NULL,
    embedding_model     VARCHAR(200),
    title_vector		VECTOR(768),
    embedding_vector    VECTOR(768),
    reg_dt              TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE tb_nlcf_fairytale_chunk (
    chunk_id             VARCHAR(50) PRIMARY KEY,
    fairytale_id         VARCHAR(50) NOT NULL,
    chunk_no             INTEGER NOT NULL,
    title                TEXT NOT NULL,
    chunk_text           TEXT NOT NULL,
    reg_dt               TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT uq_fairytale_chunk_no UNIQUE (fairytale_id, chunk_no)
);
